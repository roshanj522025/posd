<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0D1117">

    <!-- Terminal output -->
    <ScrollView
        android:id="@+id/terminalScroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp"
        android:scrollbars="vertical">

        <TextView
            android:id="@+id/terminalOutput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textIsSelectable="true"
            android:textSize="12sp" />
    </ScrollView>

    <!-- Quick-action row -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#161B22"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="4dp">

            <Button android:id="@+id/btnTab"
                style="@style/QuickKey"
                android:text="TAB" />

            <Button android:id="@+id/btnCtrlC"
                style="@style/QuickKey"
                android:text="Ctrl+C" />

            <Button android:id="@+id/btnCtrlD"
                style="@style/QuickKey"
                android:text="Ctrl+D" />

            <Button android:id="@+id/btnUp"
                style="@style/QuickKey"
                android:text="▲" />

            <Button android:id="@+id/btnDown"
                style="@style/QuickKey"
                android:text="▼" />
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Input row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="#161B22"
        android:padding="4dp">

        <EditText
            android:id="@+id/terminalInput"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:layout_weight="1"
            android:imeOptions="actionDone"
            android:inputType="textNoSuggestions"
            android:paddingStart="8dp"
            android:singleLine="true"
            android:textSize="13sp" />

        <Button
            android:id="@+id/btnSend"
            android:layout_width="wrap_content"
            android:layout_height="44dp"
            android:layout_marginStart="4dp"
            android:backgroundTint="#238636"
            android:text="↵"
            android:textColor="#FFFFFF"
            android:textSize="18sp" />
    </LinearLayout>
</LinearLayout>
